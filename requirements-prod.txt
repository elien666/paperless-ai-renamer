# Production requirements for Docker image
# Excludes test dependencies (pytest, pytest-*)

# Core FastAPI dependencies
fastapi
uvicorn
requests
httpx

# ML/AI dependencies (needed for production)
chromadb>=1.3.5
sentence-transformers

# Scheduler
apscheduler

# Configuration
python-dotenv
pydantic>=2.0.0
pydantic-settings>=2.0.0

